= link_to "Back to colors", :root

%table
  %thead
    %tr
      %th Color
      %th Word
      %th Votes
  %tbody
    - @votes.each do |vote|
      %tr
        %td(style="background-color: ##{vote.color}")
        %td= vote.word
        %td= vote.total
